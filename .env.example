# Local Vite proxy to staging (recommended for cookie + CORS testing)
VITE_API_BASE_URL=/api

# Direct staging origin (OAuth redirects always use this)
VITE_API_ORIGIN=https://assis-api-staging.ellwaa.com
